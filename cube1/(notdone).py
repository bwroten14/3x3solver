# prolly not necessary but organization or something


"""
from rubiks_backend import SolverEngine

engine = SolverEngine()

s_c = input("Scramble or cubestate? (enter either S or C)")
if s_c == "S" or "s":
    scramble = input("Enter your scramble: ")
    scramble = scramble.strip().upper().split()

elif s_c == "C" or "c":
    print("Enter the cubestate in this format")


import re

def parsescram(scram):
    return re.findall(r"[UDFBRL][2']?", scram.upper())

parsescram(scramble)
"""

#never got aroound to finishing this but i will at some point
#for now use commands to run rubiksbackend